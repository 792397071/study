#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int m;
	int n;
	int z=1;
	if (m < n)
	{
		z = m;
		m = n;
		n = z;
	}
	scanf("%d%d", &m, &n);
	for (int i = 0; z != 0; i++)
	{
		z = m%n;
		if (z == 0)
			break;
		m = n;
		n = z;

	}
	printf("%d", n);



}
