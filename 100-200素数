#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main()
{
	int m;
	int j;
	for (m = 100; m <= 200; m++)
	{


		for (j = 2; j <=(m/2); j++)
		{
			if (m%j == 0)
				break;


		}
		if (j > (m/2))
			printf("%d ", m);


	}

}
