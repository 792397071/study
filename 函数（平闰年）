int run(int a)
{
	if (((a % 4 == 0) && (a % 100 != 0)) || a % 400 == 0)
	return 1; 
	else
	
		return 0;
	


}

int main()
{
	for (int i = 1000; i <= 2000; i++)
	{
		if (run(i) == 1)
			printf("%d ", i);
	}
}
